#include <stdio.h>
#include "tokenizer.h"

int main(){
  puts("Welcome to your tokenizer:)");

  fputs("Enter the phrase you want to tokenize:\n,>>", stdout);
  fflush(stdout);
  char str;

  printf(str);

  return 0;
}
